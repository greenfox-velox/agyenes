import random

a = random.randint(1000, 9999)

class CowsAndBulls():
    def __init__(self, number):
        self.number = number

    def playing_the_game(number):
        if len(number) != 4:
            print("4 digit number needed")
            counter = 0
            state = "finished"
        else:
            counter += 1

    def guess(self, number):
        return guess_1

#state: playing v. finished

#For every digit that the player guessed correctly in the correct place, they have a “cow”. For every digit the player guessed correctly in the wrong place is a “bull.”
